package ru.romindous.skills.utils.pets;

public class PetUtils {/*

    private static final Field petLevel;
    private static final Field petExp;

    static {
        petLevel = PacketPlayInUseEntity.class.getDeclaredFields()[2]; //= getIdFld();
        petLevel.setAccessible(true);
        petExp = PacketPlayInUseEntity.class.getDeclaredFields()[3]; //= getIdFld();
        petExp.setAccessible(true);
    }

    public static void addPetExp(final double exp) {

    }

*/}
